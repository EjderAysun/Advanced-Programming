#include <iostream>
using namespace std;

// C++ actually adds an extra step to the basic compilation \
process: the code is run through a preprocessor, \
which applies some modifications to the source code, \
before being fed to the compiler.

int main() {
    cout<<"Hello Universe";
    return 0;
}